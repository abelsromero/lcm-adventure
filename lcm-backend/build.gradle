

dependencies {
    compile project(':lcm-model')

    compile "org.mongodb:mongo-java-driver:$mongoVersion"
    compile "org.springframework.data:spring-data-mongodb:1.7.1.RELEASE"
    compile "de.flapdoodle.embed:de.flapdoodle.embed.mongo:1.48.0"
   
}

